
# for fedora 
alias off="poweroff"
#alias vim=vimx
#walias vi=vix
PATH=$PATH:$HOME/.local/bin:$HOME/bin
export PATH
alias py=python
alias status="git status"

alias add="git add"
alias commit="life --commit"
# Docker
alias database="docker-compose -f ~/Projects/Spaceship/compose/docker-compose-database.yml -p database" # {start|up|stop|build}{name_service}
alias container="docker container"
alias inspect="docker inspect"

export PATH=$PATH:/opt/gitkraken
export PATH=$PATH:/opt/activator
PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"

#alias "git gr"= 'git log --all --decorate --oneline --graph'

# MIS ERPNEXT

alias mis="bench --site mis"
export PATH="~/.pyenv/bin:$PATH"

#export HISTORY_FILTER_EXCLUDE=("_KEY" "Bearer" "tmux")

#setopt HIST_EXPIRE_DUPS_FIRST
#setopt HIST_FIND_NO_DUPS
#setopt HIST_IGNORE_DUPS
#setopt HIST_IGNORE_ALL_DUPS
#eval "$(pyenv init -)"
#eval "$(pyenv virtualenv-init -)"
#export MANPATH="$(brew --prefix)/share/man:$MANPATH"
#export INFOPATH="$(brew --prefix)/share/info:$INFOPATH"

#bindkey -v

#bindkey '^P' up-history
#bindkey '^N' down-history
#bindkey '^?' backward-delete-char
#bindkey '^h' backward-delete-char
#bindkey '^w' backward-kill-word
#bindkey '^r' history-incremental-search-backward
#export KEYTIMEOUT=1

